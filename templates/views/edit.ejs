<%- include('../partials/header')%>
<%- include('../partials/navbar')%>
<div class="new_blog_top_margin">
    <div class="ui huge text container segment">
        <div class="ui huge header">New <%= blog.title %> </div>
        <form class="ui form" action="/blogs/<%= blog._id %>?_method=PUT" method="POST">
            <div class="field">
                <label for="">Title</label>
                <input type="text" name="newTitle" value="<%= blog.title %>">
            </div>
            <div class="field">
                <label for="">Title</label>
                <input type="text" name="newImage" value="<%= blog.image %> ">
            </div>
            <div class="field">
                <label for="">Discription</label>
                <textarea name="newBody" id="" cols="30" rows="10"><%= blog.body %></textarea>
            </div>
            <div class="field">
                <input class="ui inverted big green button" type="submit" value="submit">
            </div>
        </form>
    </div>
</div>
<%- include('../partials/footer')%>